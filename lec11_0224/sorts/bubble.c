/* Swaps `one` with `two` */
void swap(int *one, int *two) {
  int temp = *one;
  *one = *two;
  *two = temp;
}

void sort(int arr[], int n) {
  /* Fill in the code to sort the array `arr`
   * of size `n` using bubble sort */
}
